Use Gauss' law to find the field inside an
        infinite cylinder with radius $b$ and uniform
        charge density $\rho$. 
m4_ifelse(__problems,1,[:%:],[:(The external field has the same form as
        the one in problem \ref{hw:coaxcap}.):])%
\answercheck
